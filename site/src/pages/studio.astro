---
import StarlightPage from "@astrojs/starlight/components/StarlightPage.astro";
import StudioWrapper from "../components/builder/StudioWrapper.svelte";

// We use StarlightPage to get the basic head/meta tags, but we'll overlay the studio on top
---

<StarlightPage frontmatter={{ title: "Theme Studio", template: "splash" }}>
  <StudioWrapper client:only="svelte" />
</StarlightPage>
