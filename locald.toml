[project]
name = "color-system"
domain = "color-system.localhost"

[services.web]
type = "exec"
command = "pnpm docs:dev --port $PORT"

[services.vercel-demo]
type = "exec"
command = "pnpm --filter vercel-demo dev --port $PORT"
